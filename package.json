{
  "name": "@process-engine/deployment_api_meta",
  "version": "1.0.0",
  "description": "Contains the developer setup for the deployment api.",
  "main": "index.js",
  "scripts": {
    "build": "meta exec \"npm run build\" --exclude deployment_api_meta && cd _integration_tests && npm run build && cd ..",
    "postinstall": "minstall --trust-local-modules .",
    "reinstall": "./reinstall.sh"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "meta": "^1.2.18",
    "meta-npm": "^1.0.3",
    "minstall": "^3.3.1-pre3"
  }
}
